package routes

import (
	"github.com/gin-gonic/gin"
)

// 用户路由
func createOpRouter(engine *gin.Engine) {
	// var group = engine.Group("/api/bg/enrollment")
	// {
	// 	// con := &controller.EnrollmentConfig{}
	// 	// group.POST("/new", con.CreateEnrollment)
	// 	// group.GET("/:enrollmentId/info", con.GetEnrollmentById)
	// }
}
